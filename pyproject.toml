[project]
name = "chatdo"
version = "1.0.0"
description = "Director of Operations (dev-time deep agent)"
requires-python = ">=3.10"
dependencies = [
  "deepagents",
  "langchain",
  "langchain-community",
  "pyyaml",
]

[project.scripts]
chatdo = "chatdo.__main__:main"

